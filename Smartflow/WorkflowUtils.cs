using System;

using Smartflow.Elements;
using Smartflow.Enums;

namespace Smartflow
{
    /// <summary>
    /// 工作流帮助类
    /// </summary>
    public sealed class WorkflowUtils
    {
        /*
        public static XElement ConvertToRealType(ASTNode node)
        {
            if (WorkflowNodeCategeory.End == node.NodeType)
            {
                End endNode = new End();
                endNode.ID = node.ID;
                endNode.NAME = node.NAME;
                endNode.NID = node.NID;
                return endNode;
            }
            else if (WorkflowNodeCategeory.Decision == node.NodeType)
            {
                Decision dNode= new Decision();
                dNode.ID = node.ID;
                dNode.NAME = node.NAME;
                dNode.NID = node.NID;
                return dNode;
            }
            else
            {
                return node;
            }
        }
         * 
         */ 
    }
}
